```{bash}
cd ..
cd ..
cd ..
git log --oneline
git tag -am "Start" v0.0 commit
git switch git
git show
git tag key commit 
git tag -d key
git tag -am "A key feature was added to the project" commit
git switch master
```